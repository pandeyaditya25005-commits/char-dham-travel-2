# Backend API URL (Railway deployment URL + /api)
# In development, this can be empty (uses Vite proxy to localhost:5000)
# In production, set to your Railway backend URL
VITE_API_URL=https://your-railway-app.up.railway.app/api
